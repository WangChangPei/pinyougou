<?xml version="1.0" encoding="utf-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.itcast.core.mapper.user.UserDescDao">
    <insert id="save" keyProperty="cn.itcast.core.pojo.UserDesc">
        insert into tb_user_desc (username,nickName,sex,birthday,address,job) values(#{username},
        #{nickName},#{sex},#{birthday},#{address},#{job})
    </insert>
</mapper>